# Surface Code MI Scaling — 项目内容

## 项目概述

本目录研究**二维环面码（2D Toric Code）**在去相干（dephasing）噪声下的互信息（MI）与条件互信息（CMI）的标度行为，核心方法是将量子混合态的熵计算映射到经典**随机键 Ising 模型（RBIM）**的配分函数，再用张量网络收缩求解。

---

## 文件结构

| 文件 | 功能 |
|---|---|
| `h_calculation.py` | 用蒙特卡洛 + 张量网络收缩计算 `H(m)`（anyon 分布的 Shannon 熵） |
| `syndrome_pure_error.py` | syndrome 纯错误基准测试 |

---

## h_calculation.py 详细分析

### 物理背景

计算量 `H(m)` 是 anyon 配置 `m` 的 Shannon 熵期望值（对应 Eq. (9) 中的修正项）：

```
H(m) = -E_{m ~ Pr(m)} [ log Pr(m) ]
```

其中：
- `e`：物理 Pauli-Z 错误，独立以概率 `p` 作用在每条边上
- `m = ∂e`：anyon 配置（syndrome），即每个 plaquette 周围四条边错误数之和 mod 2
- `Pr(m)`：对所有能产生 anyon 配置 `m` 的错误 `e` 求边缘概率

### 算法流程

**步骤 1 — 采样错误 `e`**

对 `L×L` 的 plaquette 格子，共有：
- 水平边：`(L+1) × L` 条
- 垂直边：`L × (L+1)` 条

每条边独立以概率 `p` 采样错误 `h_errors[i,j]` 和 `v_errors[i,j]`。

**步骤 2 — 计算 anyon 配置 `m = ∂e`**

对每个 plaquette `(i,j)`，将其四条边（上、下、左、右）的错误求和 mod 2：

```python
m_grid[i, j] = (top + bottom + left + right) % 2
```

**步骤 3 & 4 — 构建并收缩张量网络**

用 `opt_einsum` 构建张量网络并收缩，得到 `Pr(m)`（对应 Appendix E，Eq. E3–E5）：

- **边权重张量**（Eq. E4）：`T_{s1,s2} = δ(s1=s2) · p^{s1} · (1-p)^{1-s1}`，每条边对应一个二腿张量
- **plaquette 宇称约束张量**（Eq. E5）：`Q^s[s1,s2,s3,s4] = δ((s1+s2+s3+s4) mod 2 == s)`
  - `Q^0`（黄色）：要求四条边错误数为偶数（anyon=0）
  - `Q^1`（绿色）：要求四条边错误数为奇数（anyon=1）

每个 plaquette 的 Q 张量按 `(top, right, bottom, left)` 顺序连接四条边的 T 张量，形成完整的因子图。

**步骤 5 — 蒙特卡洛求期望**

```
H(m) ≈ (1/N) * Σ_samples [ -log Pr(m) ]
```

### 关键函数

```
create_Q_tensor(m_val)
    输入: m_val ∈ {0, 1}
    输出: shape (2,2,2,2) 的 numpy 数组
    作用: 生成 plaquette 宇称约束张量（Eq. E5）

calculate_H_m(L=3, p=0.1, num_samples=100)
    输入: 格子大小 L、错误率 p、蒙特卡洛样本数
    输出: H(m) 的估计值（浮点数）
    作用: 蒙特卡洛 + 张量网络收缩计算 anyon 熵
```

### 依赖库

- `numpy`：数值计算
- `opt_einsum`：自动寻找最优张量收缩路径

### 默认参数

```python
calculate_H_m(L=3, p=0.1, num_samples=10000)
```

---

## 物理参数说明

| 参数 | 含义 | 典型值 |
|---|---|---|
| `L` | 格子线性尺寸（L×L plaquettes） | 3, 10, 20 |
| `p` | 物理错误率（去相干强度） | 0.05, 0.1, 0.11, 0.15 |
| `num_samples` | 蒙特卡洛采样数 | 1000–10000 |
| `max_bond` | bMPS 截断 bond 维数 | 8–64 |
| `r` | CMI 计算中缓冲区 B 的宽度 | 1–5 |

---

## 运行方式

```bash
python h_calculation.py
```

依赖安装：

```bash
pip install numpy opt_einsum quimb matplotlib
```

---

## 2026-04-15 日终汇报（P9：Eq.(12) 对比版）

### 执行环境

- 硬件：双路 `AMD EPYC 9554`，`128` 物理核 / `256` 线程
- 线程设置：`OMP_NUM_THREADS=1`、`OPENBLAS_NUM_THREADS=1`、`MKL_NUM_THREADS=1`
- 主脚本：`src/scripts/plot_cmi_vs_r_three_p_chunked.py`
- 并行设置：经 `r=8, chi=128, N=100, chunk=1` 校准后，选用 `120 workers`

### 今日完成内容

- 完成 `r=1..5` 的主生产：`p in {0.05, 0.11, 0.15}`，`chi=64`，每点 `N=5000`，输出见 `outputs/CMI_r1to5_chi64_summary.csv`
- 完成 `r=6` 的主生产：`chi=96`，每点 `N=5000`，输出见 `outputs/CMI_r6_chi96_summary.csv`
- 完成 `r=7,8` 的闸门验证：`chi=128`，每点 `N=500`；六个点全部 `mean <= 0`，因此**今日不进入 `r=7,8` 主生产**
- 对最难点 `(p=0.15, r=6)` 进行定点升 `chi`：
  - `chi=128, N=1000`：`-0.000043 ± 0.001155`
  - `chi=160, N=1000`：`-0.000947 ± 0.001106`
  - `chi=192, N=1000`：`-0.001698 ± 0.001091`
  - `chi=256, N=5000`：`0.001241 ± 0.000426`
- 结论：对 `(p=0.15, r=6)`，**当前已验证的稳定正值门槛为 `chi=256`**

### 当前最佳数据集（用于 Eq.(12) 比较）

- `r=1..5`：采用 `chi=64, N=5000`
- `r=6, p=0.05/0.11`：采用 `chi=96, N=5000`
- `r=6, p=0.15`：采用 `chi=256, N=5000`

按 `mean - 2 sem > 0` 的稳定判据：

| 点位 | 当前结果 |
|---|---|
| `p=0.05, r=1..4` | 稳定正值 |
| `p=0.05, r=5,6` | 正值但仍偏边界 |
| `p=0.11, r=1..5` | 稳定正值 |
| `p=0.11, r=6` | 正值但仍偏边界 |
| `p=0.15, r=1,2,4,6` | 稳定正值 |
| `p=0.15, r=3,5` | 正值但仍偏边界 |

### 与论文 Eq.(12) 的比较

论文给出：

$$
I(A:C|B) \simeq
\begin{cases}
e^{-r/\xi(p)} & p \neq p_c \\
r^{-\alpha} & p = p_c
\end{cases}
$$

其中 `p_c \approx 0.11`，见 `docs/PAPER.md` 与 `docs/PHYSICS.md`。

用**仅稳定正值点**做拟合：

- `p=0.05` 指数拟合：`b = -0.8026`，`R^2 = 0.9995`，点集 `r = 1,2,3,4`
- `p=0.15` 指数拟合：`b = -0.2438`，`R^2 = 0.7177`，点集 `r = 1,2,4,6`
- `p=0.11` 幂律拟合：`alpha = 0.5412`，`R^2 = 0.6787`，点集 `r = 1,2,3,4,5`

### 今日结论

1. **定性上支持论文 Eq.(12)**：
   - `p=0.05 < p_c` 的 CMI 随 `r` 清晰指数衰减；
   - `p=0.11 \approx p_c` 的衰减明显慢于两侧，更接近幂律；
   - `p=0.15 > p_c` 在修正 `r=6` 的截断偏差后，也与指数衰减图景一致。
2. **定量上尚未完全复现论文参数**：
   - `p=0.05` 分支已经较稳；
   - `p=0.15` 分支拟合质量一般，仍受有限尺寸与截断偏差影响；
   - `p=0.11` 的拟合指数 `alpha` 低于论文参考值 `~1.1`，说明 `r<=6` 仍处于明显有限尺寸区间。
3. **数值方法上的关键信息**：
   - 先前 `r=6, p=0.15` 的负值主要来自 `chi` 不足，而非物理上真实负 CMI；
   - `r=7,8` 在 `chi=128` 下未通过正值闸门，当前不宜直接纳入最终 Eq.(12) 汇报。

### 当前建议口径

- 可以将今日结果作为**“Eq.(12) 的定性复现 + `r=6` 数值门槛定位”**版本汇报；
- 暂不应宣称已在 `r=1..8` 上完成论文 Fig. 3(d) 的定量复现；
- 下一步优先不是继续盲目推进 `r=7,8`，而是先把 `r<=6` 的边界点做成稳定正值数据集。

---

## 2026-04-16 日终汇报（最终 `r=1..6` 最佳数据重拟合版）

### 执行环境

- 硬件环境：沿用 `2026-04-15` 的双路 `AMD EPYC 9554`，`128` 物理核 / `256` 线程
- 线程设置：`OMP_NUM_THREADS=1`、`OPENBLAS_NUM_THREADS=1`、`MKL_NUM_THREADS=1`
- 主生产脚本：`src/scripts/plot_cmi_vs_r_three_p_chunked.py`
- 重拟合脚本：`src/scripts/fit_final_r1to6_best.py`

### 今日补点参数（用于把 `r<=6` 全部补成稳定正值）

- `(p=0.05, r=5)`: `L=22`, `max_bond=64`, `num_samples=10000`, bMPS
- `(p=0.05, r=6)`: `L=26`, `max_bond=96`, `num_samples=20000`, bMPS
- `(p=0.11, r=6)`: `L=26`, `max_bond=96`, `num_samples=10000`, bMPS
- `(p=0.15, r=3)`: `L=14`, exact TN, `num_samples=10000`
- `(p=0.15, r=5)`: `L=22`, `max_bond=64`, `num_samples=20000`, bMPS

### 最终最佳数据集口径

- `r=1..5` 主体：来自 `outputs/CMI_stageP9_r1to5_chi64_raw.csv`
- `r=6, p=0.05/0.11`：来自 `outputs/CMI_stageP9_r6_chi96_raw.csv`
- `r=6, p=0.15`：采用高精度验证结果 `outputs/CMI_verify_p015_r6_chi256_N5000_raw.csv`
- 最终汇总表：`outputs/CMI_final_r1to6_best_summary_2026-04-16.csv`

按 `mean - 2*sem > 0` 判据，`p ∈ {0.05,0.11,0.15}`, `r=1..6` 的 `18` 个点现在**全部为 `stable_positive`**。

### 重新拟合 Eq.(12)

基于 `outputs/CMI_final_r1to6_best_summary_2026-04-16.csv` 的全 `r=1..6` 最佳数据，重新拟合：

- `p=0.05` 指数拟合：`b = -0.7761`，`R^2 = 0.9984`
- `p=0.15` 指数拟合：`b = -0.2784`，`R^2 = 0.5321`
- `p=0.11` 幂律拟合：`alpha = 0.7656`，`R^2 = 0.7076`

对应输出：

- 单独重拟合图：`outputs/CMI_final_r1to6_best_refit_2026-04-16.png`
- 新旧对比图：`outputs/CMI_final_r1to6_fit_comparison_2026-04-16.png`
- 拟合参数表：`outputs/CMI_final_r1to6_fit_comparison_2026-04-16.csv`
- 点位变化表：`outputs/CMI_final_r1to6_point_comparison_2026-04-16.csv`

### 与补点前基线的比较

以“补点前 `P9` 基线”作为对照，重建口径为：

- `r=1..5` 取 `chi=64` 原始数据的前 `20` 个 chunk（每点 `N=5000`）
- `r=6, p=0.05/0.11` 取 `chi=96` 原始数据的前 `20` 个 chunk（每点 `N=5000`）
- `r=6, p=0.15` 保持 `chi=256, N=5000` 不变

对比结果：

- `p=0.05`：`b: -0.7952 -> -0.7761`，`R^2: 0.9710 -> 0.9984`
- `p=0.15`：`b: -0.3051 -> -0.2784`，`R^2: 0.4675 -> 0.5321`
- `p=0.11`：`alpha: 0.7563 -> 0.7656`，`R^2: 0.7125 -> 0.7076`

最显著的点位变化：

- `(p=0.05, r=5)`: `0.00202 -> 0.00387`，样本数 `5000 -> 10000`
- `(p=0.05, r=6)`: `0.00202 -> 0.00156`，样本数 `5000 -> 20000`，但 `sem` 明显收紧
- `(p=0.11, r=6)`: `0.00552 -> 0.00536`，样本数 `5000 -> 10000`
- `(p=0.15, r=3)`: `0.00112 -> 0.00137`，样本数 `5000 -> 10000`
- `(p=0.15, r=5)`: `0.00070 -> 0.00102`，样本数 `5000 -> 20000`

### 今日结论

1. `r<=6` 的最终最佳数据集现在已经形成统一口径，且 `18/18` 个点全部通过稳定正值判据。
2. `p=0.05` 分支的指数衰减拟合质量在补点后明显提升，已成为当前最稳的一支。
3. `p=0.15` 分支在补点后也更一致地支持指数衰减，但 `R^2` 仍不高，说明有限尺寸/截断效应仍明显。
4. `p=0.11` 的幂律指数在今日补点后仅小幅变化，说明当前 `r<=6` 数据对临界指数的约束仍有限；可做**定性支持**，不宜做过强的定量结论。
