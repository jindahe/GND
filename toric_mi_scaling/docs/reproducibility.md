# Reproducibility

Publication assets live in `results/publication/`, adopted scaling summaries in
`results/final/`, and validation or candidate studies in `results/diagnostics/`.
New raw runs and temporary figures belong in `artifacts/`.
