# Generative Decoder

This project learns the joint distribution `p(beta, gamma)` for stabilizer-code
logical variables `beta` and syndrome variables `gamma` with autoregressive
neural models.

Source code is in `src/gnd_decoder/`; checked-in code instances are in
`data/code_instances/`; generated datasets, checkpoints, and reports belong in
`artifacts/`.
