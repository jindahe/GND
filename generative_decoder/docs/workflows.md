# Workflows

Generate a code with `python -m gnd_decoder.cli.generate_code`. Build data,
train, and evaluate with `gnd_decoder.workflows.datasets`,
`gnd_decoder.workflows.train`, and
`gnd_decoder.workflows.evaluate_decoder`. Generated files default to
`artifacts/` and can be redirected with absolute paths.
