# Status

Implemented: code generation, autoregressive training, decoder evaluation,
sampled and exact cut-MI, and structured middle-cut backends. Exact enumeration
is intentionally limited to small codes; large-code MI needs a scalable backend
and predeclared sampling protocol.
