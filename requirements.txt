# Direct dependencies for the cleaned generative decoder workflow in
# `generative_decoder/`.
#
# Installing `torch` from this file uses the default PyPI build, which will
# automatically pull the matching CUDA runtime packages when available.

torch==2.11.0
numpy>=2.0.0
networkx==3.6.1
scipy==1.17.1
pymatching==2.3.1
ldpc==2.4.1
